<form method="post" action="{{ path('app_charitycategory_delete', {'id': charitycategory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ charitycategory.id) }}">
    <button class="btn">Delete</button>
</form>
