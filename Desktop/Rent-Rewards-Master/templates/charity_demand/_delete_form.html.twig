<form method="post" action="{{ path('app_charity_demand_delete', {'id': charity_demand.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ charity_demand.id) }}">
    <button class="btn">Delete</button>
</form>
