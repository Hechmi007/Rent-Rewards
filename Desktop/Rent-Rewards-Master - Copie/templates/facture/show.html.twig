{% extends 'base.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
    <h1>Facture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facture.id }}</td>
            </tr>
            <tr>
                <th>IDproduit</th>
                <td>{{ facture.IDproduit }}</td>
            </tr>
            <tr>
                <th>Nomproduit</th>
                <td>{{ facture.nomproduit }}</td>
            </tr>
            <tr>
                <th>IDClient</th>
                <td>{{ facture.IDClient }}</td>
            </tr>
            <tr>
                <th>Prixproduit</th>
                <td>{{ facture.prixproduit }}</td>
            </tr>
            <tr>
                <th>TVA</th>
                <td>{{ facture.TVA }}</td>
            </tr>
            <tr>
                <th>Totalfacture</th>
                <td>{{ facture.totalfacture }}</td>
            </tr>
            <tr>
                <th>IDLocataire</th>
                <td>{{ facture.IDLocataire }}</td>
            </tr>
            <tr>
                <th>DateFacture</th>
                <td>{{ facture.DateFacture ? facture.DateFacture|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Adressfacture</th>
                <td>{{ facture.Adressfacture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_facture_index') }}">back to list</a>

    <a href="{{ path('app_facture_edit', {'id': facture.id}) }}">edit</a>

    {{ include('facture/_delete_form.html.twig') }}
{% endblock %}
