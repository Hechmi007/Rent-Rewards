{% extends 'base2.html.twig' %}

{% block title %}Donation
{% endblock %}

{% block body %}
	<h1>Donation</h1>

	<div class="table-responsive">
		<table class="table table-primary align-middle">
			<thead class="table table-dark">
			<tr>
				<th>Id</th>
				<td>{{ donation.id }}</td>
			</tr>
			<tr>
				<th>Pointsdonated</th>
				<td>{{ donation.Pointsdonated }}</td>
			</tr>
			<tr>
				<th>Datedonation</th>
				<td>{{ donation.datedonation ? donation.datedonation|date('Y-m-d') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a class="btn btn-primary text-white py-3 px-4" href="{{ path('app_donation_index') }}">back to list</a>

	<a class="btn btn-primary text-white py-3 px-4" href="{{ path('app_donation_edit', {'id': donation.id}) }}">edit</a>

	{{ include('donation/_delete_form.html.twig') }}
{% endblock %}
