{% extends 'base.html.twig' %}

{% block title %}here we can view all Packs you need to choice
{% endblock %}

{% block body %}

	<div class="container">
		<div class="row mb-5 align-items-center">
			<div class="col-lg-6">
				<h2 class="font-weight-bold text-primary heading">
					<center>All packs</center>


				</h2>
			</div>

		</div>

		{% for paquet in paquets %}
			{# {% if charity_demand.isValid == true %} #}

				<div class="section">
					<div class="row">
						<div class="col-12">
							<div class="property-slider-wrap">
								<div class="property-slider">
									<div
										class="property-item">
										{# lhne bch n7ott fazzet upload image bch tabde temchili  #}
										<br>
										<img src="{{ asset('/uploads/images/' ~ paquet.FileUpload) }}" style="width: 20%; height: auto;">
									</a>
									<div class="property-content">
										<div class="price mb-2">
											<span>{{paquet.NomPacks}}
											</span>
										</div>
										<div>
											<span class="d-block mb-2 text-black-50">{{paquet.Discribtion}}</span>
											<span class="city d-block mb-3">{{ paquet.EtatPacks}}
											</span>

											<br>
											<form id="by-form" method="post" action="{{ path('app_stripe', {'id': paquet.id}) }}">
												<input type="hidden" name="purchase" value="true">
												<button type="submit" class="btn btn-primary text-white py-3 px-4" >Purchase this item</button>
											</form>
										</div>
									</div>

								</div>
							</div>

						</div>
					</div>
				</div>
				
			{# {% endif %} #}

		{% else %}
			<div>
				<div style="border: 1px solid black; padding: 10px;">
					<center>No Records Founds.</center>
				</div>
			</div>
		{% endfor %}
		<div class="section">
			<div id="property-nav" class="controls" tabindex="0" aria-label="Carousel Navigation">
				<span class="prev" data-controls="prev" aria-controls="property" tabindex="-1">Prev</span>
				<span class="next" data-controls="next" aria-controls="property" tabindex="-1">Next</span>
			</div>
		</div>


	{% endblock %}