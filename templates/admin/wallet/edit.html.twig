{% extends 'base2.html.twig' %}

{% block title %}Edit Wallet{% endblock %}

{% block body %}
    <h1>Edit Wallet</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_wallet_show', {'id': wallet.id}) }}">Back to show</a>
{% endblock %}
