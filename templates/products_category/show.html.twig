{% extends 'base2.html.twig' %}

{% block title %}ProductsCategory{% endblock %}

{% block body %}
    <h1>ProductsCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ products_category.id }}</td>
            </tr>
            <tr>
                <th>Categoryname</th>
                <td>{{ products_category.Categoryname }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ products_category.Type ? 'Digitel' : 'Physical' }}</td>
            </tr>
        </tbody>
    </table>

{#    <a href="{{ path('app_products_category_index') }}">back to list</a> -->

    <a href="{{ path('app_products_category_edit', {'id': products_category.id}) }}">edit</a>

    {{ include('products_category/_delete_form.html.twig') }}#}
{% endblock %}
