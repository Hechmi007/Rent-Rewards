{% extends 'base.html.twig' %}

{% block title %}PayementCredit{% endblock %}

{% block body %}
    <h1>PayementCredit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payement_credit.id }}</td>
            </tr>
            <tr>
                <th>Montantpaye</th>
                <td>{{ payement_credit.montantpaye }}</td>
            </tr>
            <tr>
                <th>Dateremboursement</th>
                <td>{{ payement_credit.dateremboursement ? payement_credit.dateremboursement|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_payement_credit_index') }}">back to list</a>

    <a href="{{ path('app_payement_credit_edit', {'id': payement_credit.id}) }}">edit</a>

    {{ include('payement_credit/_delete_form.html.twig') }}
{% endblock %}
