<form method="post" action="{{ path('app_payement_credit_delete', {'id': payement_credit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payement_credit.id) }}">
    <button class="btn">Delete</button>
</form>
